# STT Ping Log — Current Queue

This is the fast dev⇄test channel. Keep it short.

Rules:
- Only current, actionable handoffs live here.
- When an item is answered or superseded, replace it with the latest state.
- Evidence details belong in STT reports/artifacts; old ping history remains in git.
- Every active line ends with the current ball: `=> dev`, `=> test`, `=> product`, or `=> product-ops`.

Last hygiene reset: `2026-06-06T14:55Z`.

## Active

| Updated UTC | ID | Status | Latest Evidence / Ask | Ball |
|---|---|---|---|---|
| 2026-06-06T14:12Z | SELFHOST-MODELS-MAXDEPTH | **FAIL / dev action** | Base opt-in setup/transcription succeeds, but max-depth persists. Latest artifact: `/private/tmp/stt-maxdepth-console-wrapper-base-h1_6.json`; WER `0`, journey/detail pass, `Maximum update depth exceeded` = `12`, `Initializing transformers-js` = `5`. In-page `console.error` wrapper proves React does **not** emit component-stack args in this Vite/browser build; waiting for `at <Component>` lines will stall. Best path: use the persisted Zustand/update path and narrowed consumers (`SessionPage` → `StatusNotificationBar` / `MobileActionBar`; `useSessionLifecycle` data-model-status observer). | `=> dev` |
| 2026-06-06T14:25Z | V4-RELEASE-PATH | **reactivated / needs candidate** | Product reactivated v4 for release investigation. Current evidence: explicit v4 full-auth app lifecycle previously passed once (`/private/tmp/stt-v4-realbrowser-h1_6.json`, non-empty, saved/detail, WER `0.375`) before device telemetry; device-telemetry rerun then proved WebGPU worker path (`resolvedDevice=webgpu`, `backend=webgpu`, `fallbackOccurred=false`) but failed every inference with `invalid data location: undefined for input "a"` and no save (`/private/tmp/stt-v4-device-proof-092617c5-autosetup.json`). Standalone probe works on main-thread WebGPU; app worker WebGPU fails. Next dev action: supply a release-candidate integration path or explicit decision among main-thread WebGPU prototype, worker/ORT workaround, different export/runtime, or guarded experimental path with known limits. Test can re-run once a new candidate is named. | `=> dev/product` |
| 2026-06-06T14:55Z | REVIEWER-REPORT-DISPOSITION | **aligns with corrections** | Independent review aligns on controlled-soft-release `CAVEATED GO` and broad-public `HOLD` until ops/selfhost close. Current corrections: (1) config-gate-relax is now on `main` (`101bbbf1`), so missing Stripe/Sentry no longer blocks boot and payment surfaces hide/guard checkout when Stripe is missing. (2) v4 is reactivated by product, but current app-worker WebGPU evidence still fails with `invalid data location`; it needs a new candidate path before test can green it. (3) selfhost remains unmerged/active, but now has both diagnostic zero-HF proof and signed-in short proof; see `SELFHOST-MODELS`. | `=> dev/product/product-ops` |
| 2026-06-06T14:55Z | SELFHOST-MODELS | **PASS / ready for land decision** | Diagnostic zero-HF proof passed: `/private/tmp/selfhost-zero-hf-dropin-base-h1_6-20260606T1240Z.json` (`0` HuggingFace requests, model assets from origin). Signed-in short proof now passed after a pre-close evidence safeguard: `/private/tmp/selfhost-signedin-shortproof-base-h1_6-rerun.json` (`releaseProofEligible=true`, real auth on `5174`, runner/gate pass, WER `0` / `100%`, transcript/detail match, session persisted, `whisper-base.en`, no cloud fallback). First signed-in run `/private/tmp/selfhost-signedin-shortproof-base-h1_6.json` is invalid only because the page context was destroyed during proof cleanup/navigation; warm rerun did not reproduce. Not a model failure. | `=> dev/product` |
| 2026-06-06T13:20Z | RELEASE-OPS | **pre-release ops** | Stripe: prod must use live Stripe key or hide/disable payment conversion surfaces. Feedback DB: product-ops should apply existing `user_issue_reports` migrations; dev fallback is to hide/disable `Report issue` only if migrations will not be applied before release. These are ops/release-surface tasks, not STT model bugs. | `=> product-ops/dev` |

## Recently Closed / Superseded

| Updated UTC | ID | Result |
|---|---|---|
| 2026-06-06T14:12Z | MAXDEPTH-COMPONENT-STACK-REQUEST | Answered. In-page wrapper captured only warning string + JS/Zustand stack; no React component-stack args exist in this run. |
| 2026-06-06T13:34Z | CONFIG-GATE-RELAX | PASS and landed on `main` (`101bbbf1`). With Supabase env only and Stripe/Sentry unset, `/auth/signup` and `/pricing` boot instead of `Configuration Required`; `stripeKeyClass=missing`; pricing exposes no checkout button, only `Start Free`. |
| 2026-06-06T13:09Z | CONSOLE-TO-LOGGER | PASS. Static `console.*` guard clean; production-preview public-route console audit clean. |
| 2026-06-06T13:20Z | FRIENDLY-COPY-ITEM8 | Closed as stale. Friendly mode labels landed in `738b1929`; no current test work unless product asks for a new copy pass. |
| 2026-06-06T13:20Z | PRIV-DOWNLOAD-CONSENT | Closed as stale. Model-aware consent landed in `738b1929`; base.en size corrected to ~80 MB in `1c567431`. |
| 2026-06-06T05:18Z | RELEASE-UX-COPY | Code-side copy fixes complete: stale `Vault Mode` removed from `frontend/src`; auth-wall CTAs routed/copy-adjusted. |

## Deferred / Lower Priority

| ID | State |
|---|---|
| ZERO-HF-CI | Useful hardening hook after selfhost-models lands; not a blocker for the current max-depth fix. |

2026-06-06T14:40Z | dev->test | LANDED | DONE | main@101bbbf1 | Product owner directed landing of verified branches to stop stale reviews. MERGED to main (clean, eslint src=0, 150/150 affected suites): dev/console-to-logger (your 13:09 PASS — no-console enforced + raw console.* routed to logger) and dev/config-gate-relax (your 13:34 PASS — boots without Stripe/Sentry; payment surfaces hidden when stripeKeyClass=missing). So an independent review of main now reflects: console-noise closed in code, and the Stripe/Sentry boot hard-gate removed. NOT landed (correctly): dev/maxdepth-fix (still failing), dev/v4-recovery (experimental). PENDING YOUR/OWNER CALL: dev/selfhost-models — verified (0 HF + signed-in WER=0) but landing it is the FIRST Git-LFS push to GitHub (~130MB) and makes base fail-closed unless deploy runs `git lfs pull`; want confirmation on LFS-on-GitHub + deploy step before pushing binaries. dev/repetition-risk-detector — your 04:53 said land on merits; can land on request. => test
2026-06-06T14:40Z | dev->test | SELFHOST-MODELS-MAXDEPTH | REQ | dev/maxdepth-instrument@3de1625e (local) | Per your 14:12 (React emits NO component stack in this build) I pivoted to a store-mutation trace to pin the looping FIELD. Branch = current main (config-gate so it BOOTS + console-to-logger) + maxdepth Part 1/2 cherry-picked (reproduces the post-coalescing loop). Adds a flag-gated useSessionStore.subscribe trace -> window.__MAXDEPTH_TRACE__ (every mutation's changed keys+values) + window.__MAXDEPTH_SUMMARY__() (per-field counts). No console (no-console enforced); inert unless enabled. RUN: (1) checkout dev/maxdepth-instrument; (2) BEFORE app load, page.evaluate localStorage.setItem('ss.maxdepth.trace','1'); (3) fresh-cache base opt-in h1_6 on 5174 real-auth through the max-depth window (DOWNLOAD_REQUIRED->ENGINE_INITIALIZING); (4) dump window.__MAXDEPTH_SUMMARY__() and the first ~80 entries of window.__MAXDEPTH_TRACE__ to an artifact. The field with a runaway count (or an A->B->A oscillation in the entries) is the loop driver — that pins it without a component stack. If the top field is modelLoadingProgress despite Part 2, the coalescing path is being bypassed; if it is a DIFFERENT field (e.g. sttStatus/privateModelStatus/isReady), that's the real culprit and I'll fix that. => test

2026-06-06T15:05Z | dev->test | CI-UNIT-SHARD1 | DONE | main@4a5bb6b7 | Root-fixed the unit-shard-1 + unit-coverage failure (yours was the symptom fix on the one mock). Root cause = my console-to-logger change dropped the import.meta.env.DEV guard on the [STORE-IDENTITY] marker, so logger.debug ran at useSessionStore MODULE INIT; any test mocking `logger` without `.debug` crashed on import (default.debug is not a function). Restored the dev-only guard (commit 2997223d) so it never runs in test/prod — prevents the NEXT incomplete logger mock from breaking CI too; instance id still on window.__LAST_STORE_ID__. Verified UserFillerWordsManager + useSessionStore 24/24, eslint 0. Thanks for extracting the log + the mock alignment. => test
2026-06-06T15:05Z | dev->test | LANDED-2 | DONE | main@4a5bb6b7 | Per owner: landed dev/repetition-risk-detector (your 04:53 "land on merits") — non-mutating repetition-risk evidence flag; repetitionRisk 6/6 + SpeechRuntimeController 21/21 + eslint 0. Branch hygiene per owner: deleted all merged branches + stale experiments; remaining dev/* = maxdepth-fix, maxdepth-instrument (your store-trace run pending), selfhost-models (pending owner LFS/deploy OK). All other dev work is now on main or deleted. => test

2026-06-06T15:25Z | dev->test/product | SELFHOST-MODELS | LANDED | main@4d07f118 | Per owner ("all completed branches merged"), landed dev/selfhost-models to main. Clean merge (no file overlap), LFS push OK (5 objects / 169MB uploaded to GitHub LFS — confirms LFS enabled + within quota). Private v2 now loads LOCAL-ONLY from /models/ (allowRemoteModels=false, no HF fallback) for BOTH tiny (default) and base (opt-in); both are Git LFS. IMPORTANT DEPLOY GATE: this is fail-closed — if the Vercel build does not materialize LFS (serves 130-byte pointers instead of real .onnx), ALL Private STT breaks (no HF fallback now), not just base. Prior Vercel LFS smoke proved Vercel serves real binaries, but since this is the first LFS on main, please VERIFY on the next production deploy that GET /models/whisper-tiny.en/onnx/*.onnx and /models/whisper-base.en/onnx/*.onnx return real ONNX (multi-MB), not pointers, and run a quick prod Private tiny + base smoke. Production Canary on this push should also exercise it. If Vercel serves pointers, we must enable LFS checkout in the Vercel build (git lfs pull). => test/product

2026-06-06T16:10Z | dev->test | V4-RELEASE-PATH | REQ | dev/v4-recovery@ac9484c4 (local) | Per product: v4 narrowed to a TIME-BOXED Path-1 spike (worker+WebGPU, probe-first), park if no clean landing. VALUE-PROP GATE (confirmed): v4-tiny is NOT a product win — v2 already ships WebGPU via whisper-turbo, and @huggingface/transformers isn't even in main deps; v4 only justifies as a bridge to better models the v2 runtime can't run. So this spike only proves the next-gen runtime works IN OUR WORKER, not tiny speed. DIAGNOSIS: diffed our v4 worker vs HF's OFFICIAL working `realtime-whisper-webgpu` example — same device(webgpu) + dtype(fp32/q4); the ONE material delta is the decode path: HF uses direct non-chunked generate (no chunk_length_s, no return_timestamps), ours uses the chunked ASR pipeline (chunk_length_s + stride + return_timestamps). The `invalid data location: undefined for input "a"` likely comes from a chunked/strided intermediate tensor under WebGPU-in-worker. PROBE (probe-first): frontend/v4-worker-webgpu-probe.html — self-contained, CDN import, runs whisper-tiny.en on device=webgpu inside a Web Worker and A/Bs both decode paths. RUN: serve it (e.g. on 5174) in a real WebGPU Chrome, click "Run probe", paste window.__V4_WORKER_PROBE__ JSON. Verdicts: PASS-A (A ok, B fails) => chunked pipeline is the breaker, I adopt non-chunked + external <=30s windowing in the app worker; BOTH-OK => config delta (env flags/version); FAIL-A => no clean worker-WebGPU landing => PARK v4 for this release (ship v2 tiny default + base opt-in). => test
