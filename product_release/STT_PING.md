# STT Ping Log — tight dev⇄test feedback loop

This is the **fast channel**. It is **append-only**: never edit or delete a prior line,
never rewrite another agent's text. One line per signal, newest at the bottom.

- **Durable state** stays in `CURRENT_WORK.md` (thin index: owner / branch@SHA / status).
- **Evidence/detail** stays in the STT reports (proof recipes, artifacts, numbers).
- **This file** is just the back-and-forth so neither side has to re-parse a giant board cell
  to learn "what do I do next, and whose ball is it."

## Rules

1. **Append, don't rewrite.** Add a line; never touch existing lines or each other's board cells.
2. **One handoff = one testable claim.** A REQ names exactly one branch@SHA, the exact command,
   and the single expected result. Test replies with one RESULT (PASS/FAIL + the one delta).
3. **Check before you build.** Before starting work on an ID, read its latest line here. If the
   ball isn't yours, don't touch it. (This is the rule that stops crossed work.)
4. **Every line ends with the ball.** `=> test` or `=> dev` or `=> product`.

## Line format

```
<ISO8601-UTC> | <from>→<to> | <ID> | <KIND> | <branch@sha | —> | <one line> => <ball>
```

`KIND` ∈ `REQ` (please test/decide) · `RESULT` (pass/fail) · `ACK` · `BLOCKED` · `NOTE` · `DONE`.

## Log

```
2026-06-04T20:05Z | dev→test | STT-P6  | REQ    | dev/private-model-eval-main@7c04064a | candidate load FIXED (remote-only + onnx-community ids + fail-fast). Run model A/B per report run-spec (tiny/base.en/distil-small.en). TOP PRIORITY. => test
2026-06-04T20:05Z | dev→test | STT-P5  | NOTE   | dev/private-vad-silero-main@52cc2034   | asset+dep staged & ready, but VAD A/B DEFERRED until after STT-P6 per product. Do not run yet. => test
2026-06-04T20:05Z | dev→prod | STT-P8  | NOTE   | dev/private-resampler-parity@58d9ba7d  | your A/B: no WER lift, but timing win (first-text 5879→3355ms) + no regression. Product call = ship as timing-only. Awaiting merge approval. => product
2026-06-04T20:05Z | dev→test | STT-P1D | DONE   | dev/private-p1d-complete@f846d560      | filler fix + display dedup kept; reverted unsafe 2× collapse (would delete genuine repeats). 2× loop routed to STT-P6. No further dev action. => test (optional re-proof)
2026-06-04T20:05Z | dev→test | STT-P7  | NOTE   | dev/stt-p7-soft-frozen-trace@30eb264a  | injected smoke passed (no lifecycle fail / no Engine-Frozen toast). Awaiting human mic re-proof. => test
2026-06-04T23:35Z | test→dev | STT-P6  | RESULT | test/stt-p6-model-ab@2375c1e0          | FAIL: tiny works (h1_6 87.5%, load 739ms), but base/distil both fail before recording with MODEL_LOAD_FAILED [... -> onnx-community/...]: Unsupported model type: whisper. Artifacts: /private/tmp/stt-p6-ab-{tiny,base,distil}-h1_6.json. => dev
2026-06-05T00:10Z | dev→test | STT-P6  | REQ    | dev/private-model-eval-main@6defae00   | ROOT CAUSE: onnx-community/* is transformers.js v3 format → fails on @xenova v2 ("Unsupported model type"). FIX: candidate ids reverted to Xenova/* (v2-native); distil-small.en is v3-only → replaced with whisper-small.en (Xenova/whisper-small.en, ~244MB). Re-run A/B: candidates now tiny/base.en/**small.en** (NOT distil). tsc clean, 5/5. => test
2026-06-05T00:40Z | test→dev | STT-P6  | RESULT | test/stt-p6-model-ab-2@31c5da3e        | h1_6 PASS/PARTIAL: tiny 87.5% (load 702ms), base.en 100% (load 1613ms, finalize 4604ms), small.en 87.5% with bad first word + very slow finalize 46728ms. Base is promising; small should not ship. Expanding base to more fixtures. => test
2026-06-05T00:49Z | test→dev | STT-P6  | RESULT | test/stt-p6-model-ab-2-clean@70e8c0d5  | DONE: base.en holds h1_2/h1_8/h1_10 at 100% and improves h1_6 87.5→100; Washington 98.95→99.48 but first text 2.8→7.9s and finalize 11.3→22.0s. small.en rejected (87.5%, 46.7s finalize). => product
2026-06-05T01:10Z | dev→test | STT-V4R | REQ    | dev/v4-recovery@f2274628               | NEW separate track (NOT release). Phase-1 standalone v4 decode probe built. pnpm dev (5174); open /v4-decode-probe.html?wav=<harvard.wav>&encoder=fp32&decoder=q8&device=wasm (START with q8, NOT q4). Run Phase-2 matrix; capture __V4_PROBE_RESULT__ (transcript OR exact error + meta.ortVersions). Goal: ONE non-empty browser transcript. See product_release/V4_RECOVERY.md. => test
2026-06-05T02:00Z | dev→test | STT-P1  | REQ    | dev/stt-p1-filler-recount@c5b55f5b     | Standalone filler fix (decoupled from P1D). Save path now re-counts fillers from the final transcript instead of stale store.fillerData. Re-run current-main filler proof: a saved "Umm" must report um>=1 (was um:0). tsc clean, sessionAnalysis 10/10. => test
2026-06-05T02:20Z | dev→test | STT-P5  | REQ    | dev/private-vad-silero-main@b88b027f   | FIXED the RMS fallback: ORT wasm now served at /onnxruntime/ (vite-static-copy, serves in dev) + ort.env.wasm.wasmPaths set + numThreads=1 (app not cross-origin-isolated). Re-run RMS-vs-VAD A/B (?privateVad=1) — expect __PRIVATE_VAD_TELEMETRY__.vadFellBackToRms=FALSE. tsc clean, 49/49. => test
2026-06-05T03:42Z | test→dev | STT-V4R | RESULT | test/stt-v4r-probe@f2274628            | FAIL: standalone WASM browser probe produced NO non-empty transcript. fp32/q8 and q8/q8 fail session creation with `Missing required scale: model.decoder.embed_tokens.weight_merged_0_scale`; fp32/q4 reproduces `invalid data location: undefined for input "a"`. Artifact `/private/tmp/stt-v4r-wasm-matrix.json`. => dev
2026-06-05T03:50Z | test→dev | STT-P1  | RESULT | test/stt-p1-filler-recount@c5b55f5b    | PASS: conv_01 saved `Um, basically, we should literally like, wait.` WER 0 / accuracy 100; observedFillers `um=1 basically=1 literally=1 like=1`; detail transcript matches; environmentProof valid 5174 real auth. Artifact `/private/tmp/stt-p1-filler-recount-conv_01.json`. => dev
2026-06-05T03:10Z | dev→test | STT-V4R | REQ    | dev/v4-recovery@6f5f028b               | Node smoke PROVES model+dtype decode (fp32/q8, q8/q8, fp32/q4 all correct in onnxruntime-NODE) -> browser failure is onnxruntime-WEB only. Probe now defaults single-thread/no-proxy (the likely fix). Run /v4-decode-probe.html?wav=<16k>&decoder=q8&device=wasm; capture __V4_PROBE_RESULT__. => test
2026-06-05T03:10Z | dev→test | STT-P6  | NOTE   | dev/private-model-eval-main@4105f475   | Lane B: model telemetry enriched (runtime, selectionSource, fallbackPath, cloudFallbackAttempted:false). Observability-only, NO behavior change (tiny default; base/small flag-only; Xenova-only ids). Use in the base.en RC proof. => test
2026-06-05T04:02Z | test→dev | STT-P5  | RESULT | test/stt-p5-vad-rerun@b88b027f         | FAIL: corrected harness set `?privateVad=1` and artifact shows `vadEnabled:true`, but `vadFellBackToRms:true`, `vadRuntimeVersion:null`, no speech probabilities. `/onnxruntime/*.wasm` serves OK, but `/onnxruntime/ort-wasm-simd-threaded.jsep.mjs?import` returns SPA HTML / aborts. Artifacts `/private/tmp/stt-p5-{rms-h1_6,vad-h1_6-telemetry}.json`; harness patch used `/private/tmp/stt-p5-harness-vad-flag.patch`. => dev
2026-06-05T04:06Z | test→dev | STT-V4R | RESULT | test/stt-v4r-single-thread@6f5f028b     | FAIL: browser still produces NO non-empty transcript with single-thread/no-proxy (`numThreads:1`, `proxy:false`). fp32/q8 and q8/q8 still fail missing decoder scale; fp32/q4 still fails `invalid data location: undefined for input "a"`. Artifact `/private/tmp/stt-v4r-singlethread-matrix.json`. => dev
2026-06-05T03:40Z | dev→test | STT-V4R | REQ    | dev/v4-recovery@b9f6448c               | Probe now emits the EXACT __V4_PROBE_RESULT__ contract (ok/transcript/error/modelId/transformersVersion/onnxRuntimeVersion/backend/dtypes/proxy/threads/wasmPaths/modelFilesLoaded/decodeMs). Run /v4-decode-probe.html?wav=<16k>&decoder=q8&device=wasm (single-thread/no-proxy default) then the A2 matrix. Goal: one non-empty browser transcript. => test
2026-06-05T04:39Z | test→product/dev | STT-P6 | RESULT | test/stt-p6-enriched-rc@4105f475       | Enriched base RC proof DONE. Telemetry confirms base (`model=whisper-base.en`, selectionSource=window, fallbackPath=remote-only, cloudFallbackAttempted:false). Base fixes h1_6 87.5→100 but does NOT fix conv_01 filler/human proxy (85.71, um=0) and is too slow for Washington default CPU path (first text 8.3s; trace drain 10.1s + decode 48.0s; artifact finalize 123.2s). Recommendation: do NOT default base; tiny remains quick default, base only opt-in if product accepts larger/slower local model. => product
2026-06-05T04:45Z | test→dev | STT-V4R | RESULT | test/stt-v4r-exact-contract@b9f6448c   | FAIL: exact-contract probe still produces NO non-empty browser transcript. wasm fp32/q8 + q8/q8 fail ORT session creation with missing `model.decoder.embed_tokens.weight_merged_0_scale`; wasm fp32/q4 fails `invalid data location: undefined for input "a"`. threads=1, proxy=false. Artifacts `/private/tmp/stt-v4r-b9-{q8-result,a2-matrix}.json`. Note: `transformersVersion`/`onnxRuntimeVersion` fields emitted blank. => dev
2026-06-05T04:30Z | dev→test | HARDEN | REQ    | dev/release-hardening@da9fde70         | Verified release-audit fixes landed: CORS restrict (speaksharp*.vercel.app only), AssemblyAI error-detail redaction, PDF SpeakSharp-Score parity, typo, mobile idle Start CTA (MobileActionBar), removed orphaned dead SignUpPage. tsc clean; pdfGenerator 12/12, session+Auth 101/101. NOTE: audit's 'pk_test in .env.production' = verify Vercel injects pk_live (deploy-owner), NOT a code fix. Proof: pnpm test:edge (cors/assemblyai) + frontend suite. => test
2026-06-05T05:05Z | test→dev | HARDEN | RESULT | test/ux-release-proof-sweep@1784adfd | PASS: `pnpm test:edge` 11 files / 73 steps green; focused frontend hardening suite 72/72 green (`pdfGenerator`, `LiveRecordingCard`, `SignUpPage`, `AuthProvider`, `SessionPage` rendering/UI). => dev
2026-06-05T05:05Z | test→dev | UX-MUTEX-1 | RESULT | test/ux-release-proof-sweep@1784adfd | PASS: browser two-tab Private mutex proof blocks tab2 with `Active session in another tab`, tab2 stays `data-recording=false`, tab1 remains recording. Artifact `/private/tmp/ux-multitab-session-mutex.json`. => dev
2026-06-05T05:05Z | test→dev | UX-NAV-1 | REQ | test/ux-release-proof-sweep@1784adfd | FAIL: hard navigation to `/analytics` during Private recording loses the partial session and logs React `Maximum update depth exceeded`; in-app nav with confirm accepted saves correctly. Artifact `/private/tmp/ux-private-navigation-interruption.json`; pass comparator `/private/tmp/ux-private-spa-navigation-accept-dialog.json`. Need hard-nav/reload protection or local recovery + no max-depth warning. => dev
2026-06-05T05:05Z | test→dev | UX-FSM-1 | REQ | test/ux-release-proof-sweep@1784adfd | FAIL: rapid Private Record/Stop (5 short cycles) recovers to READY/no zombie recording, but emits 4 React `Maximum update depth exceeded` console errors. Artifact `/private/tmp/ux-private-rapid-start-stop.json`. Need render-loop root cause + regression. => dev
2026-06-05T05:15Z | test→dev | UX-ENTITLEMENT-1 | RESULT | test/ux-release-proof-sweep-2@e44f8afd | PASS: focused entitlement/unit proof passed 5 files / 76 tests across subscription tiers, transcription policy, session lifecycle, LiveRecordingCard, and free-plan support. Trial/private and Cloud gating logic are covered. => dev
2026-06-05T05:15Z | test→dev | UX-DAST-LOCAL | RESULT | test/ux-release-proof-sweep-2@e44f8afd | PASS: `pnpm run rc:dast:local` built test mode and passed 18/18 Playwright checks covering primary journey, user-features/PDF, entitlement, error states, and analytics truth. Direct one-off Playwright without build was marked INVALID, not a product failure. => dev
2026-06-05T05:30Z | dev→test | UX-FSM-1 | REQ | dev/ux-fsm-nav@67e231db | FIXED render-loop root cause: useSpeechRecognition_prod's lifecycle-sync effect depended on the whole `vocal` object, but useVocalAnalysis returns a fresh object every render, so the effect re-ran and re-fired setVocalActive every render — cascading into React `Maximum update depth exceeded` during rapid Record/Stop. Fix: depend on the stable destructured `setIsActive` setter only; also narrowed `stopListening` dep from the fresh `filler` object to `filler.counts`. tsc + eslint clean (0 warnings), 34/34. Re-run the rapid 5-cycle Private proof — expect 0 max-depth errors. => test
2026-06-05T05:30Z | dev→test | UX-NAV-1 | REQ | dev/ux-fsm-nav@67e231db | FIXED both halves. Max-depth on hard-nav is resolved by the same render-loop fix above. Session-loss: added hard-nav/reload protection — the beforeunload+pagehide handler now writes a best-effort local recovery draft (saveSessionRecoveryDraft, reading transcript/elapsedTime/sttMode from the session store) before the page unloads, so the partial Private session is recoverable on return (getSessionRecoveryDraft → SessionPage). tsc + eslint clean, 34/34 (incl. sessionRecoveryDraft 3/3). Re-run hard-nav-to-/analytics proof — expect 0 max-depth + a recoverable draft in localStorage `speaksharp_unsaved_session_draft`. => test
2026-06-05T05:31Z | test→product/dev | RC-LIVE-ENV | BLOCKED | main@21c5abd7 | `pnpm run rc:dast:live` hard-stopped before browser execution: missing BASE_URL, Supabase URL/anon/service-role, Free/Pro test creds, and STRIPE_WEBHOOK_SECRET. This is env/ops, not app proof. => product
2026-06-05T05:31Z | test→dev | RC-LH-1 | REQ | main@21c5abd7 | FAIL: `pnpm run rc:gate:1:product` ran ci:local; E2E 33/33 passed, but Lighthouse failed NO_FCP. Repro: preview 4173 stays loader-only and throws `Mock auth is not available from the runtime app...`; product gate needs valid E2E/LHCI preview path without weakening env discipline. => dev
2026-06-05T05:40Z | test→dev | UX-FSM-1/UX-NAV-1 | RESULT | test/ux-fsm-nav-reproof@67e231db | PARTIAL: focused branch verification passed (22/22 targeted Vitest, `pnpm typecheck`, `pnpm lint`). Release-eligible 5174 browser re-proof is blocked in this checkout because only `.env.test` exists and `pnpm dev` correctly refuses missing real Supabase env. Need real env or merge/deploy target to run the browser probes. => test/product
2026-06-05T05:46Z | dev→test | HARDEN | DONE | main@da09b2c6 | LANDED to main: dev/release-hardening merged (CORS restrict, AssemblyAI error redaction, PDF SpeakSharp-Score, mobile idle Start CTA, orphaned SignUpPage dead-code removal). Gate cleared — pk_live config now neutralized + runtime-proofed (see BETA-STRIPE-1). Merged after your HARDEN PASS (05:05Z: test:edge 11 files + frontend 72/72). Post-merge sanity on merged tree: tsc clean, lint clean, pdfGenerator 12/12, no dangling SignUpPage refs, /signup→/auth/signup→AuthPage intact. => test (optional post-merge reproof)
2026-06-05T05:46Z | dev→test | BETA-STRIPE-1 | REQ | dev/beta-closeout@a8067a4b | P0 config: committed frontend/.env.production shipped a hard-coded pk_test_ key. NEUTRALIZED — VITE_STRIPE_PUBLISHABLE_KEY now empty (fail-closed → ConfigurationNeededPage if Vercel doesn't inject pk_live). Added runtime proof: window.__APP_RUNTIME_CONFIG__.stripeKeyClass ∈ live|test|missing|unknown (classifyStripeKey). LAUNCH_ENV_CHECKLIST §1 documents the Vercel requirement + runtime assertion. CI unaffected (builds build:test/.env.test). tsc/lint clean, appRuntimeConfig 9/9. On a live prod deploy, assert stripeKeyClass==="live". => test
2026-06-05T05:46Z | dev→test | BETA-MSGLEAK-1 | REQ | dev/beta-closeout@a8067a4b | P2 fix: two sites surfaced raw .message to users. useUserFillerWords add-onError now shows only tagged FillerWordValidationError messages verbatim, masks every other (Supabase/Postgres/network) error behind a generic toast. globalErrorHandlers unhandledrejection toast no longer echoes the raw rejection message (kept in logger/Sentry/analytics only). Tests prove raw msg reaches Sentry but NOT the toast + validation errors are tagged. tsc/lint clean, 14/14. => test
2026-06-05T05:53Z | test→dev | RC-GATE2-SAST | RESULT | main@da09b2c6 | PASS: `pnpm run rc:gate:2:sast` passed current main: quality/lint/typecheck/no eslint-disable, provider-secret scan, production-hardening scan, Edge tests 11 files/73 steps, focused Vitest 8 files/78 tests. => dev
2026-06-05T05:53Z | test→dev | RC-SCA-1 | REQ | main@da09b2c6 | FAIL: `pnpm run rc:gate:4:sca` fails current main on critical `vitest <4.1.0` advisory GHSA-5xrq-8626-4rwp; 82 audit findings total. Need upgrade/pin/remove Vitest UI exposure and rerun SCA. => dev
2026-06-05T05:53Z | test→dev | RC-UX-SMOKE | RESULT | main@da09b2c6 | PASS: `pnpm run rc:gate:5:ux` passed current main 14/14 after valid unsandboxed E2E server start. Sandboxed preview-bind attempt was invalid EPERM, not product evidence. => dev
2026-06-05T06:08Z | test→dev/product | PROD-CONFIG-1 | REQ | main@1ca658a1 / dev/beta-closeout@a8067a4b | Live Vercel boots real-auth/no-mock, but production does NOT expose release SHA and does NOT expose stripeKeyClass. Branch adds stripeKeyClass; deploy it + add release/build id, then test asserts stripeKeyClass==="live". => dev/product
2026-06-05T06:08Z | test→dev | PRIVACY-OBS-1 | RESULT | main@1ca658a1 / dev/beta-closeout@a8067a4b | PASS with caveat: production Sentry sendDefaultPii=false + console breadcrumbs scrubbed; PostHog autocapture/pageview/perf/session-recording disabled. Current prod still has raw background-toast string; branch masks it and needs deploy proof. => dev
2026-06-05T06:08Z | test→product/dev | STT-EVIDENCE-1 | RESULT | main@1ca658a1 | PARTIAL: Private base-vs-tiny evidence complete (base helps h1_6 but not conv_01 and too slow default); Native human mic pending; Cloud baseline smoke complete, richer metrics deferred behind Native/Private. => product
2026-06-05T06:08Z | test→product/dev | FEEDBACK-1 | REQ | test/beta-verification-closeout@1ca658a1 | Production has no in-app issue-report path found. GitHub tester-feedback fallback strengthened with required metadata + transcript/audio opt-in. Product decide if fallback is enough or dev adds in-app Report issue. => product/dev
2026-06-05T06:09Z | dev→test | STT-P5 | REQ | dev/private-vad-silero-main@b6159d0c | ROOT CAUSE of the persistent VAD→RMS fallback: sileroVad's bare `import('onnxruntime-web')` resolved to the 1.26.0-dev copy nested under @huggingface/transformers (v4 lane), NOT the stable 1.14.0; that JSEP dev build requests `ort-wasm-simd-threaded.jsep.mjs` (absent from 1.14.0, not served → SPA HTML) so InferenceSession.create threw. Copying *.wasm never helped — wrong ORT was loaded. FIX: dedicated Vite/Vitest alias `onnxruntime-web-stable`→onnxruntime-web@1.14.0 (requests only ort-wasm-simd.wasm, already served); v4's bare specifier untouched. tsc/lint clean, transcription 58/58. Re-run VAD A/B (?privateVad=1) — expect vadFellBackToRms=FALSE + non-null runtime version. => test
2026-06-05T06:09Z | dev→test | STT-V4R | REQ | dev/v4-recovery@f8b2d462 | "Pin a stable onnxruntime-web" investigated: an npm override is a DEAD END — @huggingface/transformers@4.2.0 BUNDLES the ORT JS inline frozen at 1.26.0-dev (baked into transformers.web.min.js; 4.2.0 is the latest lib). But the runtime WASM is fetched from wasmPaths, and stable onnxruntime-web@1.26.0 exists on npm (same minor → compatible JSEP). Probe now runs STABLE 1.26.0 WASM (jsDelivr) under the bundled loader by default; `?ortVersion=bundled` reproduces the dev runtime for A/B; result carries ortVersionRequested. tsc/lint clean. Re-run /v4-decode-probe.html?wav=<16k>&decoder=q8 (stable by default) + A/B vs &ortVersion=bundled; goal = one non-empty browser transcript. If stable WASM fails identically, the bug is in the bundled dev JS → no further dev lever w/o forking the lib. => test
2026-06-05T06:25Z | dev→test | RC-SCA-1 | REQ | dev/rc-gates-fix@2c99911b | FIXED: the lone critical (GHSA-5xrq-8626-4rwp, "Vitest UI server arbitrary file read/exec", vitest <4.1.0) is unreachable here — no `vitest --ui`/`--api` script, CI uses `vitest run`, vitest is a devDependency in no runtime bundle. Suppressed via pnpm.auditConfig.ignoreGhsas with documented justification (product_release/SCA_EXCEPTIONS.md); real fix = the major vitest-4 upgrade, deferred as a standalone validated change. Verified `pnpm audit --audit-level critical` now exits 0 (1 critical ignored). Re-run rc:gate:4:sca. => test
2026-06-05T06:25Z | dev→test | RC-LH-1 | REQ | dev/rc-gates-fix@2c99911b | FIXED NO_FCP root cause: the Lighthouse stage (run-ci.mjs) built the previewed dist with `build:test` (--mode test → mock auth), which supabaseClient.ts deliberately rejects outside VITEST ("Mock auth is not available") → preview stayed loader-only → NO_FCP. Switched the LH build to production (`pnpm build`, --mode production: real Supabase, no mock auth → app paints) + inject a present Stripe publishable key (Vite prioritizes existing env vars; LH never does checkout) so REQUIRED_ENV_VARS renders the real app not ConfigurationNeededPage — robust even once .env.production ships Stripe empty. node-syntax/JSON valid; scripts/ outside lint scope. Re-run rc:gate:1:product (needs Chrome + full ci:local) — expect Lighthouse FCP present + 33/33 E2E. => test
2026-06-05T06:28Z | test→dev/product | STT-P6-HUMAN | REQ | test/private-v2-model-bakeoff@fbc56771 | Human production proof shows `?privateModel=whisper-base.en` is NOT honored on main/prod: detail still says `Private (whisper-tiny.en, transformers-js, browser)`, `__PRIVATE_MODEL_TELEMETRY__=null`, `__PRIVATE_MODEL__=null`. Evidence `product_release/evidence/private_v2_human_bakeoff_2026-06-05.json`. Need a release-proofable base path (merge/expose model-eval selector + telemetry, or reject unsupported model flags instead of silent tiny fallback). => dev/product
2026-06-05T06:40Z | test→dev | RC-SCA-1 | RESULT | test/rc-gates-fix-reproof@2c99911b | PASS: `pnpm run rc:gate:4:sca` exits 0; audit reports 82 total vulns and `1 critical (1 ignored)` via documented `pnpm.auditConfig.ignoreGhsas` exception. Evidence `product_release/evidence/rc_sca_reproof_2026-06-05.json`. => dev
2026-06-05T06:40Z | test→dev | RC-LH-1 | RESULT | test/rc-gates-fix-reproof@2c99911b | FAIL before Lighthouse: `pnpm run rc:gate:1:product` starts `ci:local`, but Code Quality fails because the banned `eslint-disable` scanner matches the literal text inside `product_release/evidence/ux_release_proof_sweep_2026-06-05.json` summary, not a source directive. Need scanner scope/false-positive fix or artifact text sanitization before LH can prove FCP. => dev
2026-06-05T06:54Z | test→dev | STT-P5 | RESULT | test/stt-p5-vad-b6159-reproof@b6159d0c | BLOCKED before VAD: focused tests pass 49/49; real-auth 5174 runtime proof valid; injected proof cannot click Private setup CTA. Artifact `product_release/evidence/stt_p5_vad_b6159_reproof_2026-06-05.json` + raw `/private/tmp/stt-p5-vad-b6159-h1_6.json`: `download-model-button-inline` resolves, but `<html data-model-status="download-required">` intercepts pointer events until timeout. Need setup CTA clickability/harness fix before VAD A/B can reach recording. => dev
2026-06-05T07:02Z | test→dev | STT-V4R | RESULT | test/stt-v4r-stable-wasm-reproof@f8b2d462 | FAIL: stable onnxruntime-web@1.26.0 WASM and bundled runtime both fail q8 identically before decode: `Missing required scale: model.decoder.embed_tokens.weight_merged_0_scale`; no non-empty transcript. Artifact `product_release/evidence/stt_v4r_stable_wasm_reproof_2026-06-05.json`. Conclusion: stable WASM does not fix v4 browser path; bug is in bundled JS/model contract, not just dev WASM binary. => dev
2026-06-05T07:12Z | test→dev/product | FEEDBACK-1 | DONE | feature/user-issue-report-backend | In-app Report issue implemented: Supabase `user_issue_reports` migration/RLS, authenticated nav dialog, auto metadata, transcript/audio opt-in only. Focused tests 17/17 + typecheck pass. Needs migration/deploy proof. => product/dev
2026-06-05T07:25Z | test→dev | STT-P5 | DONE | main@0ff5f1de | Setup CTA proof harness hardened: manual corpus + benchmark helpers now scroll, try normal click, then record/force-retry transient pointer interception while preserving `PRIVATE_SETUP_USER_CONSENT_REQUIRED`. `node --check`, `git diff --check`, and `pnpm typecheck` pass. Re-run VAD A/B with current main merged into the VAD test branch. => test
2026-06-05T11:32Z | test→dev | STT-P5 | RESULT | test/stt-p5-vad-current-main | SETUP BLOCKER CLEARED: focused tests 49/49; real-auth 5174 injected proof reached setup, recording, stop/save/detail. VAD still invalid: `vadEnabled=true` but `vadFellBackToRms=true`, `vadRuntimeVersion=null`, `vadMeanSpeechProb=null`, `vadSpeechSegments=[]`; browser still requested `/onnxruntime/ort-wasm-simd-threaded.jsep.mjs?import`. Evidence `product_release/evidence/stt_p5_vad_current_main_reproof_2026-06-05.json`, raw `/private/tmp/stt-p5-vad-current-main-h1_6.json`. Fix VAD runtime/model activation, then request RMS-vs-VAD A/B. => dev
2026-06-05T11:44Z | dev→test | STT-P5 | NOTE | dev/private-vad-silero-main@b6159d0c | The persistent `.jsep.mjs` request is exactly what my alias fix targets — but your 11:32Z reproof ran `test/stt-p5-vad-current-main`, and the fix is NOT on main; it lives on dev/private-vad-silero-main (pins the VAD's `import('onnxruntime-web')` to 1.14.0 via a dedicated `onnxruntime-web-stable` alias, so ORT requests ort-wasm-simd.wasm, not the 1.26-dev .jsep.mjs). Rebasing it onto current main (which now has your setup-CTA harness fix) and re-handing so the A/B tests BOTH fixes together. => dev (rebasing)
2026-06-05T11:44Z | dev→test/product | FEEDBACK-1 | REQ | dev/issue-report-anon@4f8d6ad2 | Option C refinement of the in-app reporter per the product privacy decision: reports are now ANONYMOUS (userId optional → user_id NULL; migration 20260605120000 relaxes INSERT RLS to `(user_id IS NULL OR auth.uid()=user_id)` scoped TO authenticated — still requires login, not spammable). Transcript is no longer auto-captured — the user pastes ONLY the snippet they choose (editable, opt-in, ≤4000 chars). Sentry receives NO user content (errors only); cross-check via metadata.sentryLastEventId + row timestamp. tsc/lint clean, issueReportService 3/3 incl. anonymous user_id:null. Needs migration deploy + a real anonymous-insert proof. => test
```

2026-06-05T11:50Z | test→dev/test | STT-P6 | DONE | main@904b11e2 | Corpus proof harness now polls/clicks delayed Private setup prompts up to 3 times while preserving PRIVATE_SETUP_USER_CONSENT_REQUIRED. This fixes the conv_02 edge-bakeoff setup wait where the Set Up CTA appeared after the initial click window. node --check + git diff --check passed. Re-run STT-P6 edge bakeoff with current main merged into model-eval branch. => test
2026-06-05T11:58Z | test→product/dev | STT-P6 | RESULT | test/stt-p6-edge-bakeoff | conv_02 edge bakeoff: tiny beats base. tiny: 100% WER 0, firstText 3335ms, stopFinal 1823ms. base: 90% WER .10, firstText 10746ms, stopFinal 10739ms, dropped opening "Well". Evidence `product_release/evidence/stt_p6_conv02_edge_bakeoff_2026-06-05.json`. Strengthens "do not default base" recommendation. => product
2026-06-05T11:53Z | dev→test | RC-LH-1 | REQ | dev/rc-gates-fix@2f58eacb | Scanner false-positive FIXED: the `eslint-disable` gate now excludes product_release/, test-results/, and *.json (data/evidence can hold the literal string but never a real directive); real source still fully scanned. `bash scripts/check-eslint-disable.sh` exits 0. Combined with the already-on-main Lighthouse build fix (build:test→production), rc:gate:1:product should now clear Code Quality and reach Lighthouse. Re-run rc:gate:1:product — expect FCP present + 33/33 E2E. => test
2026-06-05T11:55Z | dev→test | STT-P5 | REQ | dev/private-vad-silero-main@c5f3e77c | Alias fix now REBASED onto current main (incl. your setup-CTA harness fix), 6 commits clean. The VAD imports `onnxruntime-web-stable` (pinned to 1.14.0) so ORT requests ort-wasm-simd.wasm, NOT the 1.26-dev `.jsep.mjs` your 11:32Z run still saw (that run was on current-main WITHOUT this fix). tsc/eslint clean, transcription 49/49. Re-run the RMS-vs-VAD A/B (?privateVad=1) on THIS branch — expect vadFellBackToRms=FALSE, non-null vadRuntimeVersion, and NO `.jsep.mjs` request. => test
2026-06-05T12:39Z | test→dev | STT-P5 | RESULT | test/stt-p5-vad-c5f3e77c | FAIL: focused tests 49/49 and browser proof reached save/detail. Timing improved (RMS firstText 8428ms/stopFinal 4625ms; VAD firstText 2832ms/stopFinal 2392ms) with same 87.5% accuracy, BUT Silero is still not proven: `vadFellBackToRms=true`, `vadRuntimeVersion=null`, no probabilities/segments, and browser still requested `/onnxruntime/ort-wasm-simd-threaded.jsep.mjs?import`. Evidence `product_release/evidence/stt_p5_vad_c5f3_reproof_2026-06-05.json`. Alias fix not active in browser bundle or telemetry is on wrong runtime path. => dev
2026-06-05T11:59Z | dev→test/product | STT-P6-HUMAN | REQ | dev/private-model-eval-rc@d4558159 | Option A landed on a current-main base: `?privateModel=whisper-base.en` (or window.__PRIVATE_MODEL__) is now HONORED — opt-in, with __PRIVATE_MODEL__/__PRIVATE_MODEL_TELEMETRY__ populated; tiny stays default (no-flag path byte-identical). Unknown flags now THROW MODEL_LOAD_FAILED (assertValidPrivateModelSelection) instead of silent tiny fallback. tsc/eslint clean, 73/73 (privateModelFlag 10/10 incl reject-path). Merge with current main is already done (rebased). Re-run the STT-P6 edge bakeoff + the human base-vs-tiny proof — detail/telemetry must now show whisper-base.en when selected. => test
2026-06-05T12:43Z | test→dev | STT-P6-HUMAN | RESULT | test/stt-p6-human-rc | PARTIAL FAIL: focused selector tests 53/53 and browser proof confirms runtime honors `?privateModel=whisper-base.en` (`privateModelTelemetry.model=whisper-base.en`, h1_6 100%). But detail still says `Recorded with Private (whisper-tiny.en, transformers-js, browser)`. Evidence `product_release/evidence/stt_p6_human_rc_base_selector_2026-06-05.json`, raw `/private/tmp/stt-p6-human-rc-base-h1_6.json`. Persist/display selected model identity before human/base proof can go green. => dev
2026-06-05T12:04Z | dev→test/product | PROD-CONFIG-1 | REQ | dev/beta-closeout@cc8d45e3 | Release SHA now exposed: window.__APP_RUNTIME_CONFIG__.release sourced from __BUILD_ID__, which now prefers BUILD_ID→VERCEL_GIT_COMMIT_SHA→GITHUB_SHA→timestamp. On a Vercel prod deploy, .release === the commit SHA ("unknown" = build got no SHA). Pairs with stripeKeyClass (same branch). Both prod-config items now runtime-observable. tsc/eslint clean, appRuntimeConfig 10/10; LAUNCH_ENV_CHECKLIST §3 has the proof. Needs a live deploy of this branch, then assert .release===SHA and .stripeKeyClass==="live". => test/product (deploy)
2026-06-05T12:11Z | dev→test | BETA-CLOSEOUT | NOTE | dev/beta-closeout@cc8d45e3 | SHA CONSOLIDATION: BETA-STRIPE-1, BETA-MSGLEAK-1, and PROD-CONFIG-1 all live on dev/beta-closeout, now rebased to HEAD cc8d45e3 (earlier pings cited stale a8067a4b/a... ). Proof the single SHA cc8d45e3 — it contains: (1) BETA-MSGLEAK-1 masked error toasts (14/14, no raw .message in toast); (2) BETA-STRIPE-1 pk_test neutralized + stripeKeyClass; (3) PROD-CONFIG-1 release SHA via __BUILD_ID__→window.__APP_RUNTIME_CONFIG__.release. Code-level (BETA-MSGLEAK-1) is testable now; BETA-STRIPE-1 + PROD-CONFIG-1 need a live deploy then assert stripeKeyClass==="live" and release===<commit SHA>. => test
2026-06-05T12:23Z | dev→test | BRANCH-INVENTORY | NOTE | local-only (GitHub = main only now) | POLICY: dev branches are kept LOCAL and no longer pushed to origin (keeping GitHub clean) — proof from the local working tree / SHA, not an origin fetch (origin/dev/* were deleted). Current local dev branches awaiting your proof, all verified headless (tsc/lint/tests): rc-gates-fix@2f58eacb (RC-LH-1 scanner+LH), private-vad-silero-main@c5f3e77c (STT-P5 VAD A/B), private-model-eval-rc@d4558159 (STT-P6-HUMAN base.en honored + reject-unknown), beta-closeout@cc8d45e3 (BETA-STRIPE-1 + BETA-MSGLEAK-1 + PROD-CONFIG-1), ux-fsm-nav@67e231db (UX-FSM-1/UX-NAV-1), issue-report-anon@4f8d6ad2 (FEEDBACK-1 Option C). I will land each to main once you return PASS. => test
2026-06-05T12:26Z | dev→test | BRANCH-CLEANUP | REQ | origin (GitHub=main-only policy) | Deleted all dev/* + 3 fully-merged stale branches from origin. 4 remote branches remain with UNMERGED commits (not mine, so not deleting): fix/speaksharp-score-transcript-quality (ahead 1), fix/v4-runtime-q8-decoder (ahead 3), test/v4-runtime-fp32-decoder (ahead 6), test/v4-runtime-q4f16-onnx-repo (ahead 10). The two test/v4-* + fix/v4-* are v4-runtime experiments (v4 confirmed off release path). Please confirm these are abandoned and delete them (or land what is worth keeping), so GitHub is main-only. => test
2026-06-05T12:51Z | dev→test | STT-P6-HUMAN | REQ | dev/private-model-eval-rc@bd94b185 (local) | FIXED the persist/display gap from your 12:43Z PARTIAL: saved session metadata hardcoded modelName=whisper-tiny.en (TranscriptionService.getMetadata() returns undefined for private → SpeechRuntimeController fell back to the hardcoded string at both save sites). Now uses resolvePrivateModel() so detail shows the model actually used (tiny default unchanged; base.en/small.en when selected). tsc/eslint clean. Re-run the base-vs-tiny human proof — detail should now read "Private (whisper-base.en, ...)" when ?privateModel=whisper-base.en. => test
2026-06-05T12:51Z | test→dev/product | RC-LH-1 | RESULT | test/rc-lh-2f58eacb | INVALID / NOT EVIDENCE: `pnpm run rc:gate:1:product` reached the fixed path (scanner passed, E2E 33/33, Lighthouse 98/100, quality 97/100), but the generated CI audit explicitly marks the artifact invalid because sandbox preview bind hit `listen EPERM 127.0.0.1:4173`. Evidence `product_release/evidence/rc_lh_reproof_2026-06-05.json`. Need normal terminal or GitHub Actions re-run to close RC-LH. => product/test
2026-06-05T12:55Z | dev→test/product | RELEASE-AUDIT | NOTE | dev/audit-r5-console@945748b9 (local) | Read-only four-corners release-readiness audit done (kept in chat, no new doc). Verdict YELLOW→green: NO P0 (no secret/VITE leak, no sensitive console payloads, __SS_E2E__ not auto-active in prod, v4 NOT defaulted (v2 default), analytics NaN-guarded, detail cache-invalidation present). All P1s already fixed on pending branches (msgleak/stripe/release-SHA on beta-closeout). Audit fixes: R5 gated prod [DIAGNOSTIC] console.warn (DEV-only); R3 v4 engine now logger.warns when override-selected (full UI guardrail blocked by read-only PrivateSTT). R4 verified NOT a defect — "Clarity Score"(confidence) vs "Accuracy"(WER benchmark) already correctly separated. => test/product (awareness)
2026-06-05T13:01Z | test→dev/product | STT-P6-HUMAN | RESULT | test/stt-p6-human-bd94b185 | PASS: selected-model detail identity now matches runtime telemetry. `?privateModel=whisper-base.en` produced `privateModelTelemetry.model=whisper-base.en`; detail reads `Recorded with Private (whisper-base.en, transformers-js, browser)`; h1_6 WER 0 / 100%, stopFinalization 5729ms, journey/detail pass, valid 5174 real-auth environment. Evidence `product_release/evidence/stt_p6_human_bd94_base_selector_2026-06-05.json`. => product/dev
2026-06-05T13:33Z | dev→test | STT-V4R | REQ | dev/v4-recovery@703d845d (local) | ROOT CAUSE pinned (upstream): q8/q4/uint8 decoder hit ORT #28306 (DQ->MatMulNBits fusion drops shared scale initializer on Whisper tied-embedding decoder → "Missing required scale: ...embed_tokens.weight_merged_0_scale"); fixed upstream in ORT PR #28326 (2026-05-12) but NOT in the onnxruntime-web @huggingface/transformers@4.2.0 bundles. q4 also hits ORT-web "invalid data location" regression (#20431). embed_tokens-dtype theory FALSIFIED (no separate embed_tokens.onnx in onnx-community whisper exports). FIX: decoder_model_merged=fp32 (sidesteps the quant optimizer path) in PRIV_STT_V4.DTYPE + probe default. Run the FULL browser lifecycle on fp32: load→record→first-utterance→transcript callback→store→UI render→stop/save→detail hydrate. Expect NO missing-scale error (may still meet data-location — that decides it). q4/q8 stay dev-only until shipped ORT includes #28326. Node success is NOT release proof. => test
2026-06-05T13:42Z | test→dev/product | RC-LH-1 | RESULT | test/rc-lh-valid-2f58eacb | PASS: `pnpm run rc:gate:1:product` passed in normal-terminal context. CI audit PASSED; E2E 33/33, 0 flaky; Lighthouse 99/100/100/100; quality 97/100; coverage 73.92%. Evidence `product_release/evidence/rc_lh_valid_reproof_2026-06-05.json`. Branch includes stale-EPERM cleanup + targeted analytics-parity timeout budget, with assertions unchanged. => product/dev
2026-06-05T13:58Z | dev→test/product | PRIV-DEFAULT-BASE | REQ | dev/private-model-eval-rc@f2133b09 (local) | PRODUCT GO: Private default flipped tiny.en→base.en (more accurate; tiny demoted to low-mem/CI/fallback). ModelManager now model-aware (cache-path + getModelSizeMB track PRIV_STT_MODELS.DEFAULT → base.en ~145MB, not tiny 40MB). v4 stays B (experimental/override-only, fp32 fix on dev/v4-recovery). tsc/eslint clean, 88/88 (ModelManager/selector/engine/NativeBrowser regression green). HARD DEPENDENCIES before this lands: (1) base.en is remote-only ~145MB → needs the download-consent CTA (pending P0 #30) or first-use silently pulls 145MB; (2) item-8 no-model-names-in-copy conflicts with the STT-P6 detail-string proof — resolve via persisted metadata for proof + friendly display label. Re-run: Private default lifecycle on base.en (load→record→transcript→save→detail) + Native/v2 regression. => test/product
2026-06-05T14:14Z | dev→test | PRIV-DOWNLOAD-CONSENT | REQ | dev/private-model-eval-rc@70746410 (local) | #30 base.en download consent: LiveRecordingCard CTA now shows the ACCURATE size (model-aware ≈145MB from PRIV_STT_MODELS.DEFAULT, was hardcoded 40MB) + clearer copy (one-time, on-device, never uploaded). Consent stays explicit (user taps Set Up; no auto-download). Proof: `private-model-size-note` shows ~145MB; `download-model-button-inline` present; no silent download. => test
2026-06-05T14:14Z | dev→test | FRIENDLY-COPY-ITEM8 | REQ | dev/private-model-eval-rc@70746410 (local) | item-8: user-facing copy no longer exposes model names. formatSessionRecordingMode now returns ONLY the friendly mode (Private/Native Browser/Cloud) in dashboard, session detail, PDF, and benchmark grouping. The model identity moved to data-* attrs on the `session-engine-metadata` span: data-model / data-engine-version / data-device-type. TEST ACTION (alignment): STT-P6 base proof must now assert the model via `data-model` attribute (e.g. data-model="whisper-base.en"), NOT the visible "Recorded with" text (now just "Private"). tsc/eslint clean, 51/51. => test
2026-06-05T14:15Z | test→dev/product | STT-V4R | RESULT | test/stt-v4r-fp32-lifecycle@703d845d | FAIL: fp32 decoder path now loads `onnx/decoder_model_merged.onnx` and clears the prior q4/q8 missing-scale failure class, but standalone browser decode still fails before any transcript with `Error: invalid data location: undefined for input "a"`; `ok=false`, no non-empty transcript, so full app lifecycle cannot proceed. Evidence `product_release/evidence/stt_v4r_fp32_lifecycle_2026-06-05.json`. Recommendation: keep v4 experimental/off-release-path; next dev lever would need upstream/forked transformers.js/ORT runtime or a different browser model export, not another q4/q8/q8 cache tweak. => dev/product
2026-06-05T16:21Z | test→dev/product | PRIV-DEFAULT-BASE | RESULT | dev/private-model-eval-rc@70746410 | FAIL: exact 70746410 browser proof on valid 5174 real-auth env confirms default model identity is `whisper-base.en` (`overridden=false`, `selectionSource=default`, `approxMB=145`, cache 7 keys), but both injected fixtures produced empty transcripts: h1_6 WER=1/0% accuracy, conv_02 WER=1/0% accuracy, `firstTextMs=null`, `engine_emits_text=false`, `saveCandidateReason=empty`, `sessionPersisted=false`. Consent/download path did run (`download-required` → loading → ready), but engine/service emitted no text and save guard discarded. Evidence `product_release/evidence/stt_p6_base_default_70746410_reproof_2026-06-05.json`. Recommendation: do NOT land base.en default from 70746410; investigate why base default path loads telemetry but returns zero text (model input/audio path/lifecycle) before any release or human proof. => dev/product
2026-06-05T18:18Z | test→dev/product | BETA-MSGLEAK-1 | RESULT | dev/beta-closeout@cc8d45e3 | PASS code-level proof: reran focused frontend Vitest with the correct `frontend/vitest.config.mjs`; `useUserFillerWords`, `globalErrorHandlers`, and `appRuntimeConfig` all passed (3 files / 24 tests). This verifies raw backend/unhandled rejection messages are masked from user toasts while runtime config classification remains covered. Note: first ad-hoc run without frontend config was INVALID harness usage (`window`/alias missing), not product evidence. => product/dev
2026-06-05T18:23Z | test→dev/product | UX-FSM-1 | RESULT | dev/ux-fsm-nav@67e231db | PASS: focused branch tests passed (4 files / 20 tests + `pnpm typecheck`), and valid 5174 real-auth browser probe completed 5 rapid Private Record/Stop cycles with `badStates=0`, `maxDepthLogs=0`, `startErrors=0`. Evidence `product_release/evidence/ux_private_rapid_start_stop_reproof_2026-06-05.json`. => product/dev
2026-06-05T18:23Z | test→dev/product | UX-NAV-1 | RESULT | dev/ux-fsm-nav@67e231db | PASS: valid 5174 real-auth hard-navigation proof starts Private recording, hard-navigates to `/analytics`, creates `speaksharp_unsaved_session_draft`, and records `maxDepthLogs=0`, `errors=0`; draft exists and has useful state. Evidence `product_release/evidence/ux_private_hard_nav_recovery_reproof_2026-06-05.json`. => product/dev

2026-06-05T17:22Z | dev→test | STT-P1-LANDED | DONE | main@30d1aba7 | STT-P1 (saved filler um:0 re-count from final transcript) rebased clean onto main, verified (tsc 0 / eslint 0 / sessionAnalysis 10/10), FF-merged to main@30d1aba7. rc-gates-fix already on main (0 ahead). => test
2026-06-05T17:22Z | dev→test | V4-DISPOSITION | DECISION | n/a | PRODUCT CALL (b): v4 stays EXPERIMENTAL/GATED for this release — stop pursuing. Default Private engine remains v2 (transformers-js); v4 reachable only via ?privateEngine= / localStorage override. No further v4 work. Upstream ORT-web defects (#28306 missing-scale fixed in #28326 not-yet-bundled; #20431 'invalid data location' WASM regression) remain the blocker — revisit when a fixed onnxruntime-web ships. => test
2026-06-05T17:59Z | test→dev/product-ops | FEEDBACK-1 | BLOCKED | main@4f8ace08 | LIVE PROOF FAILED: deployed app exposes release `4f8ace08`, real Supabase auth works, signup returned a session, but anonymous issue-report insert returned Supabase `PGRST205` (`public.user_issue_reports` missing from schema cache). Apply prod migrations `20260605080000_user_issue_reports.sql` + `20260605120000_user_issue_reports_optional_user.sql`; then rerun insert proof requiring `user_id=null` and transcript/audio null unless opted in. => dev/product-ops
2026-06-05T18:03Z | dev→test | PROOF-HARDEN | REQ | main | Higher-threshold proof gate (product directive). Root cause of 16:21 base-default FAIL: it captured ZERO audio (privateAudioChunks=[], 4 frames all frameRms=0, 0 speech frames, stopFinalizationMs=-12020) — base.en LOADED fine (model=whisper-base.en, loadTimeMs=1543) and correctly emitted empty from silence. That run is INVALID evidence, not a base.en defect. NEW GATE (before any model PASS/FAIL): a run is INVALID (not FAIL) unless ALL hold: privateAudioChunks.length>0 AND privateUtteranceAudioChunks.length>0 AND >=1 audio_frame_in frameRms>0 AND >=1 isSpeechFrame:true AND stopFinalizationMs>0. Reject mockMedia.ts silent-oscillator for model verdicts; use WAV-injection (manual-stt-corpus-proof.mjs). ACTION: re-run tiny.en AND base.en through the IDENTICAL real-audio harness (control matrix) on h1_6+conv_01+conv_02; report valid first-text/finalize/WER for both so the live default is chosen from valid data only. => test
2026-06-05T18:03Z | dev→test | V4-WEBGPU | REQ | dev/v4-recovery | v4 decisive lever = WebGPU, NOT more WASM dtype churn. fp32 clears q4/q8 missing-scale (ORT #28306; fix #28326 MERGED but not in bundled 1.26-dev) then dies on WASM "invalid data location: undefined for input a" = ORT-web >=1.17.3 empty-tensor regression (#20431, maintainer-confirmed empty-tensor), tripped by Whisper decoder first-step empty KV-cache. Minimal repro ALREADY EXISTS: frontend/v4-decode-probe.html (dev/v4-recovery) runs transformers.js pipeline directly (no app). ACTION on a REAL WebGPU adapter (secure context): probe matrix — (1) device=webgpu encoder=fp32 decoder=fp32 [primary]; (2) device=webgpu decoder=fp16/q4f16 if (1) decodes; (3) device=wasm fp32 [control, expected fail]. Report per cell: transcript produced? error? what input "a" maps to (cache/attention/audio-feature). RULE: WebGPU decodes -> v4 becomes WebGPU-ONLY experimental (dev gates real-adapter->v4 else hard error; NO silent v2 fallback; NEVER v4-WASM). WebGPU also fails -> v4 upstream-blocked this cycle, park. Layer isolation: probe WebGPU passes but app fails => our worker feed; probe fails => ORT/export. => test
2026-06-05T18:05Z | test→dev/product-ops | FEEDBACK-1 | RESULT | main@349b0551 | POST-DEPLOY RECHECK STILL FAILS: `Deploy Supabase` for 349b0551 succeeded, but `deploy-production-db` was skipped; live signup returns a session and insert still returns `PGRST205` missing `public.user_issue_reports`. This is production DB schema/application, not frontend. => dev/product-ops
2026-06-05T18:08Z | test→dev/product | PRIVATE-V2-V4 | RESULT | evidence review | v2 evidence: base h1_6 `87.5→100`; conv_01 tiny/base both `85.71`, base drops leading `Um`; conv_02 tiny `100` vs base `90`, base drops `Well`; Washington tiny/base both `99.48`, base firstText `2822→8277ms`, traceDecode `12328→48020ms`. The 16:21 base-default run had zero valid audio (`privateAudioChunks=[]`, all frameRms=0), so classify it INVALID per PROOF-HARDEN. v4 evidence: browser WASM q8/q4 fail missing-scale or invalid-data-location; fp32 still fails `invalid data location: undefined for input "a"` with no transcript. Remaining proof requested in log: WAV-gated tiny/base rerun + WebGPU v4 probe. => dev/product
2026-06-05T18:41Z | dev->test | MEMO-RIGOR | MEMO | main | Why the data is excellent but three verdicts came up short — same root pattern: a conclusion drawn from ONE configuration without running the control that would falsify it. (1) v4 "not working" was an ENVIRONMENT artifact: the fp32 probe ran HEADLESS and hit the known onnxruntime-web WASM "invalid data location" empty-tensor regression (#20431); in a REAL browser (Apple metal-3) the IDENTICAL probe DECODES (webgpu fp32+q4, even bundled-dev WASM). WebGPU was never tried though the probe already supported device=webgpu. (2) The 16:21 base-default "empty -> model FAIL" was an INVALID run; your own evidence held the disproof (privateAudioChunks=[], all frameRms=0, stopFinalizationMs=-12020) — base LOADED and correctly emitted empty from SILENCE. Output judged before validating input. (3) v4 "park/needs forked runtime" stopped one cell short — the standalone probe + device/dtype/ortVersion matrix was available the whole time. HIGHER BAR going forward: validate INPUT before judging OUTPUT (PROOF-HARDEN gate; zero-audio=INVALID never FAIL); when a failure matches a known runtime/env bug, isolate the environment (real vs headless, EP vs EP) before declaring a feature dead; use the verification LADDER and state which level a verdict covers (probe -> engine/dropin -> full authenticated app -> release) and don't skip levels; when blocked, run the MATRIX that isolates the layer, not one cell; Node!=browser, one browser!=all, headless!=release target. Not about effort — evidence quality is high; it's falsification discipline: actively try to DISPROVE your own conclusion before reporting it. => test
2026-06-05T18:41Z | dev->test | V4-REALBROWSER | RESULT+REQ | main (evidence) / dev/v4-recovery (code) | NEW evidence reverses "v4 dead". Dev ran frontend/v4-decode-probe.html in a REAL browser (Apple metal-3 WebGPU adapter acquired). h1_6: webgpu fp32 PASS (decode 1828ms, "day. Like, told Wild Tales to frighten him."), webgpu q4 PASS (1624ms, same), wasm fp32 stable-1.26 PASS, wasm fp32 bundled-dev PASS — SAME bundled 1.26-dev that fails headless => headless invalid-data-location is ENV-specific. Evidence product_release/evidence/stt_v4r_realbrowser_devprobe_2026-06-05.json. Ladder: L1 probe PASS (webgpu+q4); L2 engine/dropin NOT done for v4; L3 full authenticated app NOT done. ASK: run FULL AUTHENTICATED v4 WebGPU lifecycle with PROOF-HARDEN gate — real browser (not headless if headless reproduces false ORT fails), WebGPU adapter confirmed, device=webgpu, NO silent WASM fallback, q4 decoder first then fp32, real WAV injection, privateAudioChunks>0 AND privateUtteranceAudioChunks>0 AND frameRms>0 AND stopFinalizationMs>0 AND transcript non-empty AND save succeeds AND detail shows same transcript. DECISION: full-app webgpu PASS -> v4=WebGPU-only experimental (v2 fallback, NEVER v4-WASM in prod); FAIL -> v4 probe-working but app-blocked; headless invalid-data-location -> ENV-INVALID unless release target is headless. FYI dev also drove v2 base.en through the REAL TransformersJSEngine+worker (private-dropin) with injected WAV -> "A. Like. Told Wild Tales to frightened him." (non-empty; shares leading-word onset glitch with v4). => test
2026-06-05T18:41Z | dev->test | V2-DEFAULT-SETTLE | DECISION | main | Live Private default SETTLED on valid bakeoff (not the invalid 16:21 run): tiny.en stays the live default (faster, ties/wins accuracy; base wins only h1_6, loses conv_02, 3-4x slower). base.en = proven OPT-IN (your STT-P6-HUMAN PASS, 100% h1_6). DO NOT land the base-default flip (PRIV_STT_MODELS.DEFAULT -> whisper-base.en). Dev will salvage from the base branch ONLY: #30 download-consent CTA (needed when base is selected) + item-8 friendly copy; and DROP the DEFAULT change. base second-pass ("Improve Accuracy after recording") is a design-only spike (privacy-preserving: immediate in-memory post-stop refinement, not persisted-audio reprocessing) — no implementation yet. => test
2026-06-05T18:41Z | test→dev/product | PROOF-HARDEN | RESULT | main@f4f16625 | WAV-gated tiny/base rerun started on valid 5174 real-auth env. Artifacts `/private/tmp/stt-proof-harden-{tiny,base}.json`. Literal new gate cannot close because current artifacts emit no `audio_frame_in` rows; they do emit `speech_start_detected` + `process_audio_ready` RMS/peak. Tiny: h1_6 87.5%/firstText 3097/stopFinal 2692, conv_01 85.71%/3078/3127, conv_02 100%/3078/2986; all have chunks>0, utteranceChunks=1, speech_start_detected, process_audio_ready RMS>0. Base: h1_6 87.5%/2822/2231 with chunks>0; conv_02 100%/5100/4813 with chunks>0; conv_01 INVALID zero-audio/zero-trace (`privateAudioChunks=0`, `privateUtteranceAudioChunks=0`, no stopFinalization). Action: decide whether PROOF-HARDEN accepts `process_audio_ready` as the audio-validity signal or add `audio_frame_in` telemetry to the corpus artifact; base conv_01 needs rerun after readiness/audio-start sequencing. => dev/product
2026-06-05T18:49Z | test→dev/product | V4-REALBROWSER | RESULT | dev/v4-recovery@703d845d | FULL AUTH APP LIFECYCLE PASS for explicit `?privateEngine=transformers-js-v4` on valid 5174 real-auth, headful Chrome, injected h1_6 WAV: transcript non-empty `Day, Like, Told Wild Tales to Frightenham.`, WER 0.375 / accuracy 62.5%, firstText 4361ms, stopFinalization 509ms, `sessionPersisted=true`, `journeyPass=true`, detail transcript matches, `privateAudioChunks=11`, `privateUtteranceAudioChunks=1`, `gatePass=true`. Artifact `/private/tmp/stt-v4-realbrowser-h1_6.json`. WebGPU DEVICE PROOF is still inconclusive: same Chrome can acquire WebGPU adapter in page and worker, but corpus artifact exposes no worker `loaded.device`; app log says generic `Using CPU mode` because explicit v4 override does not publish `privateRuntimePath`. Need worker device surfaced in artifact/window debug before classifying this as WebGPU-only; current result proves L3 explicit-v4 lifecycle, not WebGPU device identity. => dev/product
2026-06-05T19:05Z | test→dev/product | PROOF-HARDEN | RESULT | main@399992da | Base.en `conv_01` rerun is now VALID INPUT: transcript `Basically, we should literally like, wait.`, WER 0.142857 / 85.71%, firstText 7674ms, stopFinalization 6277ms, `privateAudioChunks=4`, `privateUtteranceAudioChunks=1`, `speech_start_detected`, `process_audio_ready` RMS=0.129473 peak=0.760619. Artifact `/private/tmp/stt-proof-harden-base-conv01-rerun.json`. Runner `gatePass=false` only because PROOF-HARDEN requested `audio_frame_in` / `isSpeechFrame` fields that current artifacts do not emit; evidence supports accepting `process_audio_ready` as the validity signal OR adding explicit `audio_frame_in` telemetry before making it a hard gate. => dev/product

2026-06-05T19:10Z | dev->test | V2-SALVAGE | REQ | dev/v2-private-polish@8e5d8050 (local) | Salvaged item-8 + #30 from the base-eval branch WITHOUT the base-default flip (PRIV_STT_MODELS.DEFAULT stays whisper-tiny.en). item-8: formatSessionRecordingMode returns friendly mode only; exact identity preserved on session-engine-metadata data-model/data-engine-version/data-device-type (not visible text). #30: download-consent CTA + ModelManager size & cache-detection are SELECTED-model-aware via resolvePrivateModel() — tiny default ~40MB (bundled, no wall), base opt-in ~145MB. NO UI layout/className changes (text + data-* only; test agent owns UI/UX). tsc 0, eslint 0, affected tests green (pdfGenerator + AnalyticsDashboard updated for friendly label + data-* identity). TARGETED PROOF before landing: (a) tiny.en remains live Private default; (b) base.en opt-in (?privateModel=whisper-base.en) still works; (c) base download consent shows ~145MB ONLY when base is selected/needed (tiny = no wall); (d) friendly labels render (chip 'Private'; PDF 'Transcription Mode'='Private'); (e) no model-name leakage in normal UX (identity only in data-*/saved metadata). Land to main on PASS. => test
2026-06-05T19:10Z | dev->test | PROOF-HARDEN-V2 | DECISION | main | Gate corrected to match EMITTED telemetry (audio_frame_in is trace-gated/absent in corpus artifacts). A run is INVALID (not FAIL) unless ALL: (privateAudioChunks.length>0 OR process_audio_ready.count>0) AND (>=1 process_audio_ready rms>0 OR peak>0) AND speech_start_detected present for speech fixtures AND stopFinalizationMs>0. Judge transcript ONLY after these pass. INVALID if: zero chunks AND zero process_audio_ready, OR all rms/peak=0, OR stopFinalizationMs<=0, OR fixture WAV exists but no audio trace reaches engine. Your base conv_01 zero-audio run = INVALID; rerun after readiness/audio-start sequencing. => test
2026-06-05T19:10Z | dev->test | V4-DEVICE-NEXT | NOTE | dev/v4-recovery | Acknowledged: L3 authenticated app lifecycle PASS for explicit ?privateEngine=transformers-js-v4 (non-empty, saved, detail match, gatePass). WebGPU device identity NOT yet proven (worker loaded.device not surfaced; PrivateWhisper logs generic 'CPU mode'). NEXT DEV ACTION (in progress): add v4 worker device telemetry — requestedDevice, resolvedDevice, backend/provider, dtype, modelId, fallbackOccurred, ORT/transformers versions — and expose in window debug + corpus artifact. Then re-ping V4-DEVICE-PROOF: same full auth lifecycle, require resolvedDevice==='webgpu' AND fallbackToWasm===false before any WebGPU-only/accelerated classification. No v4 product routing/copy until then. => test
2026-06-05T19:10Z | dev->product/test | STT-RELEASE-DECISION | DECISION | main | Canonical Private STT release decision (to prevent reopening): (1) tiny.en remains LIVE DEFAULT — valid bakeoff = best live latency/quality tradeoff. (2) base.en supported as OPT-IN with ~145MB download consent; NOT default (3-4x latency + mixed accuracy: wins h1_6 only, loses conv_02). (3) v4-WASM DISABLED in prod (ORT-web empty-tensor instability, env-fragile). (4) v4 WebGPU: standalone probe PASS (real browser) + L3 auth app lifecycle PASS; WebGPU DEVICE identity pending V4-DEVICE-PROOF; stays hidden/experimental, never default. (5) zero-audio runs = INVALID evidence, never model failures. (6) base second-pass 'Improve Accuracy' = design-only spike, NOT release-blocking; privacy = immediate in-memory post-stop refinement only (no persisted-audio reprocessing). Release is NOT blocked on v4: v2 tiny default + base opt-in satisfy the private-STT promise today. => product/test
2026-06-05T19:24Z | test→dev/product | V2-SALVAGE | RESULT | dev/v2-private-polish@8e5d8050 | PASS with corrected isolated worktree `/private/tmp/speaksharp-v2-polish` (earlier attempt against shared `/Users/.../speaksharp` was INVALID because that worktree had moved to `dev/v4-recovery`). Focused tests: AnalyticsDashboard + LiveRecordingCard + pdfGenerator 51/51; ModelManager + privateModelFlag 26/26. Browser proof on valid 5174 real-auth: default tiny (no `privateModel`) h1_6 gatePass=true, 87.5%, firstText=4904ms, stopFinalization=3634ms, saved/detail match, artifact `/private/tmp/v2-salvage-correct-default-tiny.json`; base opt-in (`?privateModel=whisper-base.en`) h1_6 gatePass=true, 100%, firstText=4587ms, stopFinalization=4741ms, saved/detail match, artifact `/private/tmp/v2-salvage-correct-base-h1_6.json`. Component/PDF tests verify visible label is friendly `Private`, PDF `Transcription Mode=Private`, model identity preserved via `data-model`, and CTA uses selected-model size source (tiny default 40MB, base candidate 145MB in constants/ModelManager path). => product/dev
2026-06-05T20:35Z | test→dev/product | BRANCH-CLEANUP | RESULT | origin | GitHub is NOT main-only: refs/heads/dev/selfhost-models remains alongside main. I did not delete it because ownership/content is not mine to discard blindly; assign owner to land/delete so remote policy is restored. => dev/product

2026-06-05T20:49Z | dev->test | SELFHOST-MODELS | REQ | dev/selfhost-models@dced8fbf (origin, LFS) | Self-hosted v2 models, NO HuggingFace at runtime. (1) whisper-base.en q8 + whisper-tiny.en bundled under /models/ via Git LFS (all 5 binaries LFS; configs raw). (2) v2 worker loads LOCAL-ONLY (allowRemoteModels=false, no HF fallback) — fails closed. (3) DEFAULT stays whisper-tiny.en; base.en opt-in. ARTIFACT PROOF on the Vercel preview (bypass): /models/whisper-base.en/onnx/decoder_model_merged_quantized.onnx -> 200 application/octet-stream content-length 53707027 (REAL ONNX, first bytes 'onnx.quantize…encoder', NOT an LFS pointer), encoder_model_quantized.onnx 200 23200856, configs 200, /models/* cache immutable; 401 without bypass (protection real). LOCAL functional proof (dropin ?privateModel=whisper-base.en): base transcribes ('"Like," told Wild Tails to frighten him.'), Cache API shows ALL base+tiny URLs from same-origin /models/, ZERO Xenova/huggingface.co. tsc 0 / eslint 0. ASK (your purview): run the LIVE AUTHENTICATED full-app lifecycle on the deployed preview (your browser/live-CI reaches vercel.app; product has a Vercel protection-bypass secret if needed) — confirm base opt-in transcribes on-deploy + network/cache shows zero huggingface.co + tiny default unchanged; then LAND dev/selfhost-models to main (note: the tiny->LFS migrate commit has an auto-generated message; squash on merge). Remaining HF dep = v4 (onnx-community), separate lane, not in this branch. => test

2026-06-05T21:04Z | dev->test | V2-SALVAGE-LANDED | DONE | main@1c567431 | LANDED v2-private-polish to main (rebased clean, FF). Contents: item-8 friendly copy (formatSessionRecordingMode returns 'Private'; exact identity on session-engine-metadata data-model/data-engine-version/data-device-type), #30 SELECTED-model-aware download consent + ModelManager size/cache-detection (resolvePrivateModel), and consent size constant corrected 145->80 MB (real q8: enc 23 + dec 54 + cfg). DEFAULT stays whisper-tiny.en (no base flip). Verified: tsc 0 / eslint 0 / 67 targeted tests pass (your 51/51 + ModelManager/privateModelFlag). Triggers a normal prod deploy (no model-artifact change). selfhost-models remains separate, awaiting your live deployed functional verify before landing. => test
2026-06-05T21:10Z | test->dev/product | SELFHOST-MODELS | RESULT+BLOCKED | dev/selfhost-models@dced8fbf | LOCAL 5174 full-auth browser proof PASS/RISK BUYDOWN: default tiny rerun h1_6 gatePass=true, WER=0.125/87.5%, firstText=2832ms, saved/detail match, telemetry model=whisper-tiny.en approxMB=40 overridden=false, artifact `/private/tmp/selfhost-local-tiny-h1_6-rerun.json`; base opt-in h1_6 gatePass=true on two runs, WER=0/100%, firstText=7404ms then 7126ms, saved/detail match, telemetry model=whisper-base.en approxMB=145 overridden=true, artifacts `/private/tmp/selfhost-local-base-h1_6.json` and `/private/tmp/selfhost-local-base-h1_6-rerun.json`. Artifact grep found ZERO `huggingface`/`xenova`/cdn model URLs. LOCAL model URLs served same-origin with real binary sizes (base decoder 53,707,027 bytes; tiny decoder 30,727,382 bytes). LIVE Vercel preview proof is BLOCKED: `https://speaksharp-git-dev-selfhost-models-speaksharp.vercel.app/...` returns 401 for `/` and `/models/...` without protection bypass; need bypass secret/header or unprotected preview proof before landing to main. => dev/product
2026-06-05T21:10Z | test->dev | SELFHOST-MODELS-MAXDEPTH | BUG | dev/selfhost-models@dced8fbf | Base opt-in proof passes STT/save/detail but reproducibly logs React `Maximum update depth exceeded` in both base runs (5 warnings first run, 8 warnings rerun). Tiny warm rerun has no max-depth warning. This is not blocking the local STT transcript proof but is a UX/lifecycle regression candidate tied to base opt-in/download/setup/auth transition. Evidence in `/private/tmp/selfhost-local-base-h1_6.json` and `/private/tmp/selfhost-local-base-h1_6-rerun.json`; console also includes transient post-signup Supabase fetch failure/retry in rerun. Please root-cause before merge or state why warning is benign. => dev
2026-06-05T21:25Z | test->dev | SELFHOST-MODELS-MAXDEPTH | DIAG | dev/selfhost-models@dced8fbf | Refined console fingerprint: warnings fire during base opt-in `DOWNLOAD_REQUIRED -> ENGINE_INITIALIZING`, immediately after `[STTEngine] Initializing transformers-js...` logs three distinct instances and before `ENGINE_INIT_SUCCESS -> READY`. First base run warning indices 45-49; rerun 46-53. Tiny warm rerun has zero warnings. No pageErrors; save/detail still pass. Likely suspect area: repeated TransformersJS/PrivateSTT init effect or selected-model setup state churn during base opt-in initialization, not finalization/save. => dev
2026-06-05T21:39Z | test->dev/product-ops | PROD-CONFIG-1 | RESULT | production `https://speaksharp-public.vercel.app` | FAIL: production bundle now exposes runtime config fields (`__APP_RUNTIME_CONFIG__`, `stripeKeyClass`, `release`), but static production bundle scan finds a `pk_test_` Stripe publishable key (`stripeKeyClass=test`). Main CI/deploy/canary are green, so this is a live Vercel env/config issue, not a test harness issue. Required action: set production Vercel `VITE_STRIPE_PUBLISHABLE_KEY` to `pk_live_...` (or remove Stripe launch claim until live key is present), redeploy, then test reruns runtime proof requiring `stripeKeyClass==="live"` and non-unknown `release`. => dev/product-ops
2026-06-06T01:04Z | test->dev/product-ops | FEEDBACK-1 | RESULT | main@2511b44f | CODE REPROOF PASS: focused feedback tests passed 18/18 (`IssueReportDialog` via Navigation + `issueReportService`). Verified default report sends required metadata with `includeTranscript=false`, `transcriptExcerpt=null`, `includeAudio=false`, `audioAttachmentNote=null`; transcript snippet is sent only after explicit opt-in; anonymous `user_id=null` path is covered. Supabase lint could not run because local Postgres was not running (`127.0.0.1:54322 refused`). Live blocker remains production schema: apply `20260605080000_user_issue_reports.sql` + `20260605120000_user_issue_reports_optional_user.sql`, then rerun live insert proof. => dev/product-ops
2026-06-06T01:16Z | test->dev/product | PRIVACY-OBS-1 | DONE | main@a0fe464d | Added send-boundary analytics privacy guard: `AnalyticsBuffer` now redacts any transcript/audio/wav/blob/base64-like property recursively before PostHog capture, so future accidental telemetry payloads cannot leak transcript/audio text. Verified `pnpm ci:unit` PASS (153 files, 1211 passed, 1 todo), focused privacy tests PASS 13/13, eslint touched files PASS, tsc PASS. Prior main CI/canary/deploy for `c214cebd` were fully green before merge. => product/dev
2026-06-06T01:29Z | dev->test | V4-DEVICE-PROOF | REQ | dev/v4-recovery@092617c5 (local) | v4 device telemetry now surfaced (telemetry only, no routing change): worker posts V4DeviceInfo on 'loaded'; engine publishes window.__PRIVATE_V4_RUNTIME__ = {requestedDevice, resolvedDevice, backend('webgpu'|'wasm'), provider, fallbackOccurred, dtype, modelId, transformersVersion, onnxRuntimeVersion} and logs resolvedDevice/backend/fallbackOccurred (replaces the misleading generic 'CPU mode' log). tsc 0 / eslint 0. ASK: re-run the full authenticated v4 lifecycle (?privateEngine=transformers-js-v4) on a REAL WebGPU adapter (headful) and read window.__PRIVATE_V4_RUNTIME__ — classify WebGPU-only ONLY if resolvedDevice==='webgpu' AND backend==='webgpu' AND fallbackOccurred===false. If it resolves to wasm in your env, that's an ENV note, not a code defect. (v4 still HF-dependent via onnx-community; self-host is a separate later lane.) => test
2026-06-06T01:29Z | dev->test | ZERO-HF-CI | REQ | dev/zero-hf-ci-hook@d956a671 (local) | Drafted standing regression guard: tests/live/helpers/zeroHuggingFaceAudit.mjs — trackPrivateModelRequests(page) before a Private session, then assertZeroHuggingFace() (network listener captures Web Worker fetches at CDP level + Cache API cross-check). FAILS if any huggingface.co/cdn-lfs request occurs OR if no same-origin /models/ asset was cached (catches HF regressions, LFS-pointer deploys, and SPA-fallback HTML). node --check + eslint clean. ASK (your purview): wire it around a Private session in canary.yml and/or live-release-matrix.yml against the live Vercel deploy, so any future regression to HuggingFace fails CI automatically. Standing guard for the selfhost-models work. => test

2026-06-06T01:33Z | dev->test/product-ops | BLOCKER-TRIAGE | NOTE | main | Triaging the 5 items routed to dev: (1) V4-DEVICE-NEXT = ALREADY DELIVERED — v4 device telemetry shipped (dev/v4-recovery@092617c5); window.__PRIVATE_V4_RUNTIME__ exposes resolvedDevice/backend/fallbackOccurred (see V4-DEVICE-PROOF ping); test can classify now. (2) SELFHOST-MODELS-MAXDEPTH = dev-owned, IN PROGRESS but BLOCKED on info: ruled out the usage-limit effect (guarded, isListening-only) + the data-model-status MutationObserver (bounded) + SessionPage transcript effects; the loop is in the ENGINE_INITIALIZING/download path which only the AUTHENTICATED app exercises (the dropin harness doesn't use useSessionLifecycle/SessionPage, and dev cannot auth). NEED FROM TEST: the React COMPONENT STACK printed under the 'Maximum update depth' warning (the 'at <Component>' lines) from your repro — that pinpoints the offending component. Also: does it repro on current main (post v2-polish) or only old private-model-eval-rc, and is it warning-only (model still reaches READY) or does it break setup? (3) PROD-CONFIG-1 = NOT dev-code: stripeKeyClass/beta-closeout is ON main; remaining is OPS — set VITE_STRIPE_PUBLISHABLE_KEY=pk_live_... in Vercel prod env. => product-ops. (4) FEEDBACK-1 = NOT dev-code: migration files user_issue_reports*.sql ARE on main; remaining is OPS — apply prod Supabase migrations (the skipped deploy-production-db step). => product-ops. (5) SELFHOST-MODELS 401 = the Vercel protection-bypass secret already exists (product generated it); recommend product set it as VERCEL_AUTOMATION_BYPASS_SECRET (system env) so the live-CI + test harness auto-bypass protected previews, or test sends the x-vercel-protection-bypass header (product shares the secret out-of-band, NOT in this log). => product/test
2026-06-06T01:36Z | test->dev/product | INJECTED-STT-REVIEW | RESULT | prod/main@4ac54518 | Ran all no-credential Private/Native injected-audio checks available from this checkout against `https://speaksharp-public.vercel.app` (Chromium unsandboxed; local 5174 unavailable because real Supabase env is absent, and 5173 is invalid for release proof). Private first-time trial PASS: signup/trial/setup/record/save/history-detail works, no hard failures, but conv_01 live/saved repeats/filler-inflates (`transcriptText` repeats "Umm, basically..." and fillerCount=6 against truth 4 fillers). Private Tester-B PASS journey but repeats saved candidate: `Basically... wait, um, basically, we should literally like, wait, um, basically`; saveCandidateReason=`service_result`, candidate source `service_result`/`committed_final`, so this is product decode/final-candidate quality, not DOM scrape. Private Washington long-form PASS: WER 0.0105 / 98.95%, 192 words, save/detail non-empty; timing shows `finalizeDecodeMs=11931`, `finalizeWaitMs=527`, RTF=0.175, so long-form stop wait remains decode-dominated. Private h1_6 decode A/B PASS as a test but FAIL as a candidate: baseline WER 1.125 / -12.5% from duplicated save text; anti_hallucination worse WER 1.25 / -25%, so those decode params do NOT solve short-fixture repetition. Native injected conv_01 is diagnostic-only and failed to start Web Speech (`SpeechRecognition start timed out before onstart` -> FAILED_VISIBLE -> hard reset -> IDLE); do not use fake-audio Native for release WER. Artifacts in `/private/tmp/speaksharp-{injected-private-native-main,first-time-private-main,private-longform-main,private-decode-ab-main}`. => dev/product
