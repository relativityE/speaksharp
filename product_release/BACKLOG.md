**Owner:** [unassigned]
**Last Reviewed:** 2026-05-13
**Version:** v0.1
**Last Updated:** 2026-05-13

# Release Backlog

This file tracks known issues, tech debt, and deferred cleanup that should not interrupt active P0/P1 release stabilization unless explicitly promoted.

## Triage Rules

| Priority | Meaning | Release Handling |
|---|---|---|
| P0 | Blocks tester release or risks billing/privacy/data integrity. | Fix before share. |
| P1 | Should fix before broader release or if it blocks validation. | Fix after active P0s are stable. |
| P2 | Workflow, maintainability, polish, or velocity debt. | Schedule after release gates are green. |

## Current Backlog

| Priority | Area | Issue | Impact | Recommended Action |
|---|---|---|---|---|
| P1/P2 | Test user workflows | `.github/workflows/setup-test-users.yml` passes `NEW_FREE_COUNT` / `NEW_PRO_COUNT`, but `scripts/setup-test-users.mjs` reads `NUM_FREE_USERS` / `NUM_PRO_USERS`. | Manual count inputs can be ignored, so the workflow may provision the default E2E/soak shape instead of the requested Basic/Pro pool. | Fix narrowly by aligning env names. Promote to P1 only if this workflow is needed for current validation. |
| P2 | Promo workflows | `.github/workflows/generate-promo.yml` and `.github/workflows/live-release-matrix.yml` both call `apply-promo/generate` with similar shell logic. | Duplicated admin-promo code can drift and create inconsistent evidence paths. | Keep both workflows for now; later extract a shared script/action or make live matrix consume a generated promo output. |
| P2 | CI performance | Split setup actions into minimal paths: `setup-node-pnpm`, `setup-playwright`, `setup-supabase`, `setup-deno-edge`, and `setup-report`. | CI Audit can exceed the improved target when edge/report setup drags, slowing iteration without implying product failure. | Optimize after release correctness gates are green. |
| P2 | Workflow consolidation | `create-user.yml` and `setup-test-users.yml` both provision admin-created users, but at different scopes. | Some evidence can be mislabeled if admin-created accounts are confused with public signup/promo evidence. | Keep both for now; document evidence type whenever used. Consolidate only if maintenance burden grows. |
| P2 | GitHub Actions maintenance | CI and RC runs emit Node.js 20 action deprecation warnings for `actions/upload-artifact@v4` / `actions/download-artifact@v4`; GitHub will force Node 24 by default on 2026-06-02 and remove Node 20 on 2026-09-16. | Not a current release blocker, but future GitHub runner changes may make artifact upload/download behavior noisy or brittle. | Schedule workflow cleanup: verify newer action versions or set/validate the Node 24 opt-in path before GitHub's cutoff. |
| P2 | CI artifact hygiene | CI health-check can warn: `No files were found with the provided path: test-results/playwright-infra/blob-report. No artifacts will be uploaded.` | Non-blocking when the health check itself passes, but warnings make it harder to spot real release issues quickly. | Make upload conditional on artifact existence or ensure the health-check step always creates the expected empty/report artifact. |
| P2 | RC/CI reporting performance | Final report aggregation and RC Gate 3 setup can take longer than the actual product test work. | Slows release feedback even when all product gates are green. | Include report-job minimization in the CI setup split: install only report dependencies and avoid unnecessary Playwright/Supabase setup for report-only work. |
| P2 | Local repo hygiene | `backend/supabase/functions/deno.lock` is present as an unrelated untracked local file. | Keeps `git status` noisy and increases the chance of accidentally including unrelated Supabase lockfile churn in release commits. | Decide whether this lockfile should be committed as canonical Supabase/Deno dependency state or ignored/removed locally; do not include it in unrelated product/UI patches. |
| P1 | Session UI polish | Session page still reads as white / near-white instead of a clearly intentional light grey canvas, and the page needs the full layout hierarchy pass: primary real-time tools above the fold should be Recording Control, Filler Words, and Live Transcript; Speaking Pace, Clarity, Pause Analysis, and tips should be secondary below. STT mode selector should live inside the Recording Control card header, and Session toasts must not cover status, controls, filler words, transcript, or mobile navigation. | The core practice page feels flatter and busier than intended, reducing visual hierarchy and adding cognitive load while users are trying to speak. | After active P0 STT validation is stable, implement the Session layout spec: full canvas/gutters visible light grey (`#F3F4F6` preferred), white cards (`#FFFFFF`) with soft borders/shadows, no Session gradients/radials, 2-column desktop grid with Recording + Filler first row, full-width Live Transcript second row, secondary metrics below, mobile stack order Recording/Filler/Transcript/Metrics, and route-aware one-toast Session placement. Validate with desktop/mobile screenshots, typecheck, focused Session tests, and primary journey E2E. |
| P1/P2 | Session loading UX | During local screenshot validation, `/session` could visibly sit on a centered amber spinner long enough to be captured before the actual cards painted. | A long obvious lazy-loading spinner on the core practice page feels broken or sluggish, especially before a user can start recording. | Measure Session route load time after build and identify whether lazy chunks, profile/usage queries, private model status checks, or auth gating are blocking first meaningful paint. Replace blank spinner-only waits with a Session skeleton/status shell if the delay cannot be eliminated quickly. |
| P1 | Basic Native UX/runtime | During the admin-created Basic live walkthrough, Session loaded and reloaded with Native selected but persistent status text showed `Error occurred` before recording; retry also surfaced `[Invariant Violation] ENGINE_ALREADY_TERMINATED` in the page. Start still changed to `Recording active`, Native selector disabled, and transcript stayed `Listening...`; stop then returned to a saved/ready state. No fresh mic-denied browser message appeared during the retry. | Basic users may see an alarming error state before taking any action, reducing trust in the only Basic STT path. | Reproduce with a real Chrome Basic account and inspect the Native initialization/status source. Fix status initialization if Native is actually ready; if mic/browser setup failed, show an actionable mic/browser message instead of generic error. |
| P1 | Basic Native analytics/save confidence | In the dedicated Chrome CDP live pass, admin-created Basic login, custom word `customboost`, and a <=20s Native recording all passed with transcript detected and no mic-denied state. However, the script did not detect an explicit saved confirmation, and Analytics showed `2 Total Sessions`, `0 WPM`, `0.0 Avg. Filler Words / Min`, and `0 mins Total Practice Time`. | Basic can appear to record, but saved/history evidence may still be low-quality or confusing for testers if metrics stay empty after a successful Native attempt. | Verify Basic session detail/history with a real manual click pass and confirm whether the Native session persisted with transcript/engine metadata. If metrics remain zero after transcript, fix analytics ingestion or minimum-duration/session-quality handling. |
| P1 | Empty session persistence | During the Basic walkthrough, a short Native recording with no transcript/real speech produced an Analytics entry with `0:00 duration`, `0 WPM`, `0 fillers`, and `0% clarity`. During the Pro walkthrough, Cloud showed `Listening...` and then displayed `Session saved` with no transcript visible after stop. | Empty sessions can pollute analytics and make users think a failed/no-speech attempt was valid practice evidence. | Confirm save threshold behavior with real mic/audio. If no transcript or below minimum duration, block save with an inline/status message, or mark the session explicitly as empty/excluded from analytics. |
| P1 | Mic permission messaging | In the in-app browser live walkthrough, Pro Session displayed a notification `Microphone access is denied. Please grant permission in your browser settings.` while the persistent status/card still showed generic `Error occurred`. | Users may miss the specific remediation if the durable session state only says `Error occurred`, especially on the critical recording page. | When mic permission is denied, keep the specific permission guidance in `StatusNotificationBar` / recording card state until resolved; avoid replacing it with generic error copy. |
| P1 | Private recording status / model assets | During the admin-created Pro live walkthrough, Private selected cleanly and Start began a timed session, but the transcript stayed `Listening...`; stopping then showed `Session saved` with no transcript visible. Browser logs during retry showed `TransformersJS` model load failures: `Unexpected token <`, suggesting a model request received HTML/index instead of model JSON. Direct deployed checks later returned `200` for `/models/whisper-tiny.en/config.json`, `/models/whisper-tiny.en/tokenizer.json`, and both quantized ONNX files, so the failure may involve stale service-worker/cache state or another generated model URL. | Pro users may see a contradictory state: Private appears active enough to save, but no private transcript is produced. This weakens trust in the privacy-first Pro path. | Reproduce after clearing site data/service worker cache and capture the exact failing model URL from browser network/logs. Keep `frontend/scripts/verify-assets.mjs` checking the full TransformersJS bundle, not only legacy Whisper-Turbo assets. If Private engine init fails, block saving empty Private sessions and keep a durable actionable setup/error state. |
| P2 | Pro history engine visibility | Follow-up deployed Chrome CDP proof resolved the prior Cloud/Private authority concern: Cloud is present and enabled in the mode dropdown, Cloud selection changes the closed selector and recording card copy to Cloud, active status reports `data-engine="cloud"` during recording, and the latest Cloud session detail shows `STT ENGINE cloud`; the following Private run similarly shows `private`. However, the Analytics history list itself still shows only session title/time/WPM/fillers/clarity/PDF, so users must open detail to confirm engine. | The privacy/trust mismatch was not reproduced, but engine identity is less visible than it should be when scanning history after Cloud/Private comparison. | Add a compact engine badge to each Analytics history row, using the same normalized label as session detail. Keep detail metadata as the authoritative source. |
| P1 | Manual mic proof environment | Codex in-app browser/manual walkthrough cannot currently be treated as real-mic proof: macOS Chrome permission does not apply to Codex.app/in-app browser, and local sandbox audio discovery reported no audio devices. Prior agent mic proof may have used a different browser surface or fake-audio automation. | Manual Native/Private/Cloud transcript evidence can be mislabeled, causing false release confidence or false blockers. | Add a mic capability preflight before any manual STT claim: browser/app name, site mic permission, `getUserMedia` success, visible device labels, real transcript from spoken phrase, and evidence label (`manual-real-mic`, `manual-ui-only`, `automated-fake-audio`, `tool-limited`). |
| P2 | Manual validation tooling | During the Basic/Pro live walkthrough, the Codex in-app browser became wedged on `/session`; navigation away and creating a clean tab timed out at `about:blank`. A later dedicated Chrome CDP session on port `9222` allowed Basic and Pro UI operation, but it still needs clear evidence labeling because Chrome was launched with mic permission auto-allow and speech stimulus came from macOS `say`. | Tool choice can change the apparent STT result, and mislabeled evidence can create false confidence or false blockers. | Prefer the dedicated Chrome CDP session for agent-operated live UI checks. Label evidence as `Chrome CDP live UI`, include account source, media setup, and artifact paths. Reserve `manual-real-mic` for a visible Chrome pass using a human-spoken phrase and normal browser mic settings. |
