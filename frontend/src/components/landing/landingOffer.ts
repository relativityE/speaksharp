/**
 * #1475 / #1470 — the complete sequential offer, stated at every signup decision point on the homepage.
 * "No card" may accompany the trial, but it never replaces the post-trial price.
 */
export const LANDING_OFFER_FULL = '30 days free, no card. Then $10/month. Cancel any time.';
export const LANDING_OFFER_SHORT = '30 days free. Then $10/month.';

/** The repository's signed-out signup destination (see the `/auth/signup` route in App.tsx). */
export const LANDING_SIGNUP_ROUTE = '/auth/signup';
